type = "select"
select = {
	{ value: "off",      en: "disable auth system" }
	{ value: "internal", en: "auth against internal database" }
	{ value: "system",   en: "system auth - eg. against /etc/shadow" }
}

title = { en: "Authentication and authorization" }

backdeps = { "users" }
