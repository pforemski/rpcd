 * always-load-me modules (for implementation firewalls of common arguments)
 * a facility like DBUS to communicate with other instances (processes) of rpcd (possibly serving the same app)
   * we need a facility like mutex to synchronize concurrent access eg. to Flatconfs /etc/fc
   * see sem_overview(7) (remember about ipcs -l)
 * rpcd<->module interface documentation for each lang

--
 * modules that connect as filters to the output of others
 * monitor and report memory usage, so admin is able to set memory limits on us
